name: CI

on:
  push:
    branches: [ "master", "v0.3.x" ]
  pull_request:
    branches: [ "master", "v0.3.x" ]

permissions:
  contents: read

jobs:
  build:
    uses: ./.github/workflows/build-matrix.yml
