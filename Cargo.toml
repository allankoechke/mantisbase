[package]
name = "mantisbase"
version = "0.4.0"
edition = "2024"
description = "Lightweight, fast, single-binary backend system"

[dependencies]
clap = { version = "4.6.1", features = ["derive"] }
spdlog-rs = "0.5"
